Reread AGENTS.md and conversation_cache before continuing. Continue from current project state.

Persist project state before ending: update current_status.md, decisions.md if needed, todo.md, known_issues.md, edge_cases.md, and datasets.md if relevant.


/config        chỉnh setting
/permissions   chỉnh quyền
/model         chọn model
/planning      lập kế hoạch trước khi làm
/fast          làm nhanh task nhỏ
/diff          xem file đã thay đổi
/tasks         xem task nền
/artifact      xem artifact 
/context       xem context
/rewind        quay lại nếu agent đi sai
/resume        mở lại hội thoại cũ
/usage         xem quota
/exit          thoát

python -m backend
npm run dev --prefix frontend

ss -ltnp 'sport = :8000'
kill 34141
