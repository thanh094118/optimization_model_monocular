# Todo

- [x] Extract WHAM `tracking_results_for_reproj` 2D keypoints and `init_betas` into `data_cam*.json`.
- [x] Blend WHAM 2D confidence into fusion `joint_confidence` before computing `K1` and `K2`.
- [x] Remove `fusion_pipeline/core.py` after moving active logic to responsibility-specific modules.
- [x] Move fusion occlusion visibility detection from `geometry.py` to `detection.py`.
- [x] Consolidate root-level cache markdown files into `conversation_cache/`.
- [ ] Perform end-to-end testing of the pipeline with the new preprocess configurations.
- [ ] Implement strict warnings when calibration mapping falls back to estimated intrinsics due to missing calibration files.
- [ ] Verify evaluation module works properly with the updated output mappings.
- [x] Redefine fusion `M` from torso-center before/after classification to parent-vector limb rotation-direction disagreement.
- [x] Split fusion logic out of `core.py` into detection, correction, and optimization modules.
- [x] Remove `hbh_pipeline` package and delete all runtime wiring from CLI/pipeline/config validation.
- [x] Add `preprocess` as a selectable stage in CLI/config loader.
- [x] Add `intrinsics_estimation` into `data_cam*.json` and intrinsics source switch in YAML.
- [x] Rename camera profile translation output field to `tvec`.
- [x] Update HBH flow to use `tvec` and `[R|t]` projection model.
- [x] Add confidence filtering parity with Colab (`conf_threshold`, `min_valid_joints`).
- [x] Add HBH method-wise evaluation module and CSV outputs.
- [x] Add HBH one-video multi-column projection visualization.
- [x] Improve HBH visualization readability (larger text, header panel, white text).
- [x] Remove `hbh_pipeline/execute.py` and migrate required logic into executor.
- [ ] Re-run full HBH stage to regenerate outputs after latest geometry/text changes. (obsolete after removal)
- [ ] Validate PA-MPJPE CSV values and final 5-column video visually on a sample clip. (obsolete after removal)
- [x] Update `learnable_pipeline/executor.py` to support sequential transition inference (`t-s -> t`) equivalent to official Learnable-SMPLify inference flow.
- [x] Validate learnable stage startup/runtime blockers (path + CPU-device issues) and apply compatibility fixes.
- [x] Review and document `optimization_pipeline/hrnet_pare_finetune.yml` optimization logic against local implementation modules.
- [x] Refactor optimization phases 2-6 into five standalone scripts driven by `optimization_pipeline/configs.yml`.
- [ ] Run end-to-end `--stage learnable` + `--stage evaluation` to measure PA-MPJPE impact after temporal mode migration.
- [ ] If `optimization_pipeline` is intended to run via `main.py`, add stage wiring in CLI choices, `config_loader.ALLOWED_STAGES`, and `pipeline.py` execution branch.
- [ ] Prepare real input payloads for standalone optimization phases and validate phase 2 -> 6 runtime in the project environment with `smplx` available.
- [x] Remove `offset_colab.py` logic and make `preprocess` default to `offset_paper`.
- [x] Integrate calibration logic to read `camera_{cam_id}.calibration` and generate both `data_{cam_id}.json` and `cameraIntrinsics_{cam_id}.pickle`.
